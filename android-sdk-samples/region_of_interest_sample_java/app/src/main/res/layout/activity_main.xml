<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".MainActivity">

    <!-- Camera preview fills the entire screen -->
    <RelativeLayout
        android:id="@+id/activity_camera_scan_frame"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent">
    </RelativeLayout>

    <!-- Result bar floats at the very bottom over the preview -->
    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/activity_camera_scan_barcode_result"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:lines="5"
        android:paddingLeft="20dp"
        android:paddingRight="20dp"
        android:paddingTop="10dp"
        android:background="#E4333333"
        android:text="@string/barcode_result"
        android:textColor="#FFFFFF"
        android:textSize="15sp"
        android:gravity="start"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent" />

    <!-- Scan button floats over the preview, just above the result bar -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_scan"
        style="@style/Widget.MaterialComponents.Button"
        android:layout_width="160dp"
        android:layout_height="48dp"
        android:text="Scan"
        android:textSize="16sp"
        android:textColor="#FFFFFF"
        android:backgroundTint="#2196F3"
        android:layout_marginBottom="16dp"
        app:strokeWidth="0dp"
        app:cornerRadius="24dp"
        app:layout_constraintBottom_toTopOf="@id/activity_camera_scan_barcode_result"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>